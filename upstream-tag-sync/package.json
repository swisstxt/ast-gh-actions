{
  "name": "upstream-tag-sync-action",
  "version": "1.0.0",
  "main": "dist/index.js",
  "devDependencies": {
    "@actions/core": "^1.11.1",
    "@actions/exec": "^1.1.1",
    "@actions/github": "^6.0.0",
    "@types/semver": "^7.5.8",
    "semver": "^7.6.3"
  },
  "description": "GitHub Action to sync repositories with upstream tags",
  "scripts": {
    "build": "bun install && bun build src/index.ts --outdir=dist --target=node"
  },
  "type": "module"
}
